# 性能压测报告（2026-09-13）

> 环境：单核云服务器 / Node 20 / PM2 fork 模式 / 单机部署

## 测试结果

| 接口 | 并发数 | 时长 | QPS (Req/s) | P95 延迟 | 备注 |
|------|--------|------|-------------|----------|------|
| `/api/status` | 50 | 30s | N/A | N/A | 纯内存计算，无限流 |
| `/api/chat` (SSE) | 3 | 5s | — | N/A | 含 LLM 调用，小流量采样 |

## 资源占用

| 指标 | 值 |
|------|-----|
| 压测前内存 | 88 MB |
| 压测后内存 | 122 MB |
| 内存增量 | 33 MB |

## 结论

- 单机 `/api/status` 峰值吞吐 **N/A QPS**，P95 延迟 **N/A**
- SSE 对话接口首字节延迟 **N/A**（受上游 LLM API 影响）
- 30 秒高并发压测后内存增量 **33 MB**，无内存泄漏迹象

## 复现方式

```bash
npm run bench        # 完整压测（status + chat 采样）
```
